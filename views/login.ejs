<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('partials/head'); %>
  </head>
  <body style="width: 100vw">
    <%- include('partials/header'); %>
    <div class="flex flex-col justify-center px-6 lg:px-8 py-12 min-h-full">
      <div class="sm:mx-auto sm:w-full sm:max-w-sm">
        <h2
          class="mt-10 font-bold text-2xl text-center text-gray-900 leading-9 tracking-tight"
        >
          Sign in to your account!
        </h2>
      </div>

      <div class="sm:mx-auto mt-10 sm:w-full sm:max-w-sm">
        <form class="space-y-6" method="POST">
          <div>
            <label
              for="username"
              class="block font-medium text-gray-900 text-sm leading-6"
              >Username</label
            >
            <div class="mt-2">
              <input
                id="username"
                name="username"
                type="text"
                autocomplete="username"
                minlength="5"
                required
                class="block border-0 focus:bg-gray-300 shadow-sm p-1.5 rounded-md w-full text-gray-900 placeholder:text-gray-400 sm:text-sm sm:leading-6 ring-1 ring-gray-300 ring-inset focus:ring-2 focus:ring-inset"
              />
            </div>
          </div>

          <div>
            <div class="flex justify-between items-center">
              <label
                for="password"
                class="block font-medium text-gray-900 text-sm leading-6"
                >Password</label
              >
            </div>
            <div class="mt-2">
              <input
                id="password"
                name="password"
                type="password"
                minlength="8"
                autocomplete="current-password"
                required
                class="block border-0 shadow-sm p-1.5 rounded-md w-full text-gray-900 placeholder:text-gray-400 ring-1 ring-gray-300 ring-inset focus:ring-2 focus:ring-inset focus:ring-gray-300 sm:text-sm sm:leading-6"
              />
            </div>
          </div>

          <div>
            <button
              type="submit"
              class="flex justify-center bg-gray-950 hover:bg-gray-600 shadow-sm px-3 py-1.5 rounded-md w-full font-semibold text-sm text-white leading-6 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-indigo-600"
            >
              Sign in
            </button>
          </div>
        </form>

        <p class="mt-10 text-center text-gray-500 text-sm">
          Not a member?
          <a
            href="/signup"
            class="font-semibold text-indigo-600 hover:text-indigo-500 leading-6"
            >Sign Up!</a
          >
        </p>

        <% if(typeof errors !== 'undefined') { %>
        <ul>
          <% errors.forEach(error => { %>
          <li class="text-red-500"><%= error.msg %></li>
          <% }) %>
        </ul>
        <% } %>
      </div>
    </div>
  </body>
</html>
